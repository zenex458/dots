#!/bin/ksh

sound="`sndioctl -n | awk 'NR == 1'`"
z=$sound

# This starts bc with a two-way pipe
bc |&
# print -p writes to the pipe
print -p scale=0
print -p $z *100
# read -p reads from the pipe
read -p vol
echo $vol
