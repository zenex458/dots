#!/usr/bin/env sh

mon=$(xrandr | awk '/HDMI/ && /connected/{print $1}' | head -n1)
connectedstatus=$(xrandr | awk '/HDMI/ && / connected/')
if [ -n "$connectedstatus" ]
then
    xrandr --output eDP1 --auto --primary --output "$mon" --auto --right-of eDP1
else
    echo "no monitor connected"
fi
